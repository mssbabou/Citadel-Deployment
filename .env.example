# Citadel Deployment Client Configuration
#
# IMPORTANT: This file contains sensitive credentials.
# - Never commit this file to version control
# - Use chmod 600 on Linux to restrict permissions
#
# To generate a strong token:
# Linux: openssl rand -base64 32
# Windows PowerShell: [Convert]::ToBase64String([Security.Cryptography.RandomNumberGenerator]::GetBytes(32))

# Authentication token — must match config.txt on the server
AUTH_TOKEN=your-very-secure-token-change-me

# Deployment server URL
DEPLOY_URL=https://deploy.example.com/deploy

# Systemd service name on the server (stopped and started around each deploy)
SERVICE=your-service-name

# Absolute path to the deployment directory on the server
DEPLOY_DIR=/opt/your-app

# Optional: default source path when no argument is passed to the script
# (Relative or absolute. Omit to default to the current directory.)
# SOURCE=/path/to/your/build
